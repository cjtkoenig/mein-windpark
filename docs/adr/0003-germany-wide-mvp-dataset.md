# Germany-wide MVP dataset

WindKlar will target a Germany-wide MVP dataset instead of a Leipzig or Saxony-only demo region. This matches the product positioning as a nationwide citizen transparency app and the MaStR source scope, while requiring the map, local cache, and import pipeline to handle clustering, filtering, and partial loading well enough for a national dataset.
