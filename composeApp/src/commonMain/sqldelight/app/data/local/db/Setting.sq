CREATE TABLE app_setting (
    key TEXT NOT NULL PRIMARY KEY,
    value TEXT NOT NULL
);

getSetting:
SELECT value
FROM app_setting
WHERE key = ?;

upsertSetting:
INSERT OR REPLACE INTO app_setting(key, value)
VALUES (?, ?);
