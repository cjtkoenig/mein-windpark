-- Add coordinate indexes for viewport map queries
CREATE INDEX IF NOT EXISTS idx_wind_park_coords
ON wind_park(latitude, longitude);

CREATE INDEX IF NOT EXISTS idx_wind_turbine_coords
ON wind_turbine(latitude, longitude);
